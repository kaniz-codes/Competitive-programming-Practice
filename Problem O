#include <stdio.h>
int main() 
{
    int N,M;
    scanf("%d", &N);
    scanf("\n%d", &M);
    if(N>M)
    { printf("Champion\n");
      printf("Runner up\n");}
    else
    { printf("Runner up\n");
      printf("Champion\n");}
    return 0;
}
