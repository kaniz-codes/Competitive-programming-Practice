#include <stdio.h>
int main() 
{
    int n,i,x,count=0;
    scanf("%d", &n);
    for(i=1; i<= n; i++)
    { scanf("%d", &x);
    if(x == 1)
    { count =1;
    break; }
    }
    if (count)
    {printf("HARD");}
    else
     {printf("EASY");}
    return 0;
}
